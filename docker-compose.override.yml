services:
    laravel.test:
        environment:
            VIRTUAL_HOST: '${VIRTUAL_HOST}'
            VIRTUAL_PORT: 80
        networks:
            - proxy

networks:
    proxy:
        external: true
